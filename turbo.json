{
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "dist/**"]
    },
    "build-app": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "dist/**"]
    },
    "build-watch": {
      "outputs": ["dist/**"]
    }
  }
}
